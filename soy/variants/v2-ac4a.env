# v2 – Access Control for All (AC4A only)
# JWT auth enforced at every microservice, not just the gateway.
ZT_AC4A=true
ZT_SR=false
ZT_MTLS=false
ZT_RA=false
ZT_RA_MS=false

ZT_PROFILES=

EXERCISE_SERVICE_URL=http://ms-exercise:8080
OTHER_SERVICE_URL=http://ms-other:8080
